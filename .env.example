# Select which auth provider to use at runtime:
# - supabase: email/password form
# - okta: embedded Okta Sign-In Widget
AUTH_PROVIDER=supabase

# Supabase (only used when AUTH_PROVIDER=supabase)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Okta (only used when AUTH_PROVIDER=okta)
# OKTA_ISSUER should look like: https://{yourOktaDomain}/oauth2/default
OKTA_ISSUER=
OKTA_CLIENT_ID=
