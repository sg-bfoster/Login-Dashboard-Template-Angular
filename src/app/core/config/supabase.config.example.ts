/**
 * Shape reference for supabase.config.ts. The real supabase.config.ts is committed
 * with blank values and overwritten from .env (SUPABASE_URL, SUPABASE_ANON_KEY) at
 * build time. See .env.example.
 */
export const supabaseConfig = {
  url: '',
  anonKey: '',
};
